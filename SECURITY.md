# 安全策略 / Security Policy

## 支持的版本

| 版本 | 是否支持安全更新 |
|---|---|
| v1.0.x (main) | ✅ 支持 |
| < v1.0.0 | ❌ 不支持，请升级至最新 Release |

> 项目当前处于 v1.0.0 初始发布阶段，`main` 分支即为受支持版本。

## 报告漏洞

如发现安全相关问题（如任意文件覆盖、ZIP 路径穿越、GUI 注入、依赖漏洞等），请按以下方式报告：

### 方式一：提交 Issue（推荐一般问题）

1. 前往 [Issues](https://github.com/KmoeFix/KmoeFix/issues) 新建 Issue
2. 标题前缀使用 `[Security]`
3. 描述中包含：
   - 影响范围与复现步骤
   - 期望行为 vs 实际行为
   - 环境信息（Python 版本 / 系统 / 安装方式）
   - 如有 PoC，请使用 `tests/test_fix_one.py:make_epub` 构造的最小内存 EPUB，**不要上传版权 EPUB 或真实用户文件**

> 适用于不涉及敏感信息泄露、可公开讨论的中低风险问题。

### 方式二：邮件报告（推荐高危/敏感问题）

如漏洞涉及敏感信息、可能被主动利用，或你希望私下披露，请邮件联系维护者：

- **收件人**：通过 GitHub 仓库主页的维护者邮箱，或提交标题为 `[Security] 私下披露` 的 Issue 后由维护者私信对接
- **邮件标题**：`[KmoeFix Security] 简要描述`
- **正文**：复现步骤、影响评估、PoC（同样需脱敏，仅用 `make_epub` 生成样本）

我们承诺：

- 收到报告后 **3 个工作日内**确认收悉
- **7 个工作日内**给出初步评估与修复计划
- 修复发布前对细节保密，发布时在 Release Notes 中致谢报告者（除非你要求匿名）

### 请勿

- 不要在公开 Issue 中直接贴出可稳定利用的完整攻击链
- 不要对非你所有的系统进行主动渗透测试
- 不要上传含版权内容的 EPUB / 图片作为样本

## 版权样本声明

- **严禁上传版权 EPUB 原文件**：Issue、PR、Discussion 中均不得附带从 Kmoe 或其他渠道获取的含版权漫画 EPUB、解压后的 `html/image` 原文件、私钥或用户路径信息。
- **测试样本规范**：所有复现与测试一律使用 `tests/test_fix_one.py` 提供的 `make_epub(path, spine_order, with_nav=True)` 在内存/临时目录中构造最小 ZIP，覆盖所需边界即可。
- 维护者会在 Review 时检查新增文件与附件，发现版权样本将直接关闭并要求移除。

## 安全实践

项目已采取以下措施降低风险：

- EPUB 处理全程离线，不发起网络请求
- ZIP 重建时 `mimetype` 固定为 `ZIP_STORED` 首条，其余 `ZIP_DEFLATED`，输出经 `get_unique_dst` 防覆盖
- 回读校验 `nums == [1..max]`，失败则删除临时文件并抛出 `回读校验失败`
- 全程 UTF-8，控制台输出经 `_safe_print` 容错，避免 GBK 注入
- 依赖仅 `tkinterdnd2`，标准库为主，减少供应链面

如有其他安全建议，欢迎通过上述渠道提交。
